[pytest]
testpaths = tests
addopts =
    --cov=src
    --cov=api
    --cov-report=term-missing
    --cov-report=html:htmlcov
markers =
    regression: tests that lock current analytics outputs from local artifacts
